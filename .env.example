# Copy to .env and fill in. Never commit .env.
# YouTube Data API v3 key — used only by scripts/update_highlights.py
# (Phase 4) to find official race-highlight video IDs. The site itself
# needs no key.
YOUTUBE_API_KEY=
